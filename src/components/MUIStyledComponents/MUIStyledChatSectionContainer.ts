import { Container } from '@mui/material';
import { styled } from '@mui/material';

export const MUIStyledChatSectionContainer = styled(Container)({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'flex-end',
});
